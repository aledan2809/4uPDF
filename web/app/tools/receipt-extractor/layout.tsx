export const metadata = {
  title: "Receipt Extractor - Extract Receipt Data to Excel/JSON | 4uPDF",
  description: "Automatically extract merchant names, dates, and amounts from PDF receipts using OCR. Batch process up to 100 receipts and export to Excel or JSON for expense tracking.",
};

export default function ReceiptExtractorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

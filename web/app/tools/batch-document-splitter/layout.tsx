export const metadata = {
  title: "Batch Document Splitter - Split Multiple PDFs by Boundaries | 4uPDF",
  description: "Upload multiple large PDFs and automatically split them by detected document boundaries. OCR-powered batch processing for invoices, orders, and multi-document files. Silver & Gold plans.",
};

export default function BatchDocumentSplitterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

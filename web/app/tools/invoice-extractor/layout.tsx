export const metadata = {
  title: "Invoice Extractor - Extract Invoice Data to Excel/JSON | 4uPDF",
  description: "Automatically extract invoice numbers, dates, company names, and totals from PDF invoices using OCR. Batch process up to 100 invoices and export to Excel or JSON format.",
};

export default function InvoiceExtractorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
